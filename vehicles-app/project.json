{
    "name": "vehicles-app",
    "$schema": "../node_modules/nx/schemas/project-schema.json",
    "projectType": "application",
    "prefix": "app",
    "sourceRoot": "vehicles-app/src",
    "tags": [],
    "targets": {
        "build": {
            "executor": "@angular/build:application",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/vehicles-app",
                "index": "vehicles-app/src/index.html",
                "browser": "vehicles-app/src/main.ts",
                "polyfills": ["vehicles-app/src/polyfills.ts"],
                "tsConfig": "vehicles-app/tsconfig.app.json",
                "inlineStyleLanguage": "scss",
                "assets": [
                    "vehicles-app/src/favicon.ico",
                    "vehicles-app/src/assets"
                ],
                "styles": ["vehicles-app/src/styles.scss"]
            },
            "configurations": {
                "production": {
                    "fileReplacements": [
                        {
                            "replace": "vehicles-app/src/environments/environment.ts",
                            "with": "vehicles-app/src/environments/environment.prod.ts"
                        }
                    ],
                    "budgets": [
                        {
                            "type": "initial",
                            "maximumWarning": "2mb",
                            "maximumError": "5mb"
                        },
                        {
                            "type": "anyComponentStyle",
                            "maximumWarning": "6kb",
                            "maximumError": "10kb"
                        }
                    ],
                    "outputHashing": "all"
                },
                "development": {
                    "optimization": false,
                    "namedChunks": true,
                    "extractLicenses": false,
                    "sourceMap": true
                }
            },
            "defaultConfiguration": "production"
        },
        "serve": {
            "continuous": true,
            "executor": "@angular/build:dev-server",
            "options": {
                "proxyConfig": "vehicles-app/proxy.json"
            },
            "configurations": {
                "production": {
                    "buildTarget": "vehicles-app:build:production"
                },
                "development": {
                    "buildTarget": "vehicles-app:build:development"
                }
            },
            "defaultConfiguration": "development"
        },
        "server": {
            "executor": "nx:run-commands",
            "options": {
                "command": "ts-node -P vehicles-app/server/server.tsconfig.json vehicles-app/server/server.ts"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint"
        },
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "vehicles-app/jest.config.cts",
                "tsConfig": "vehicles-app/tsconfig.spec.json"
            }
        },
        "serve-static": {
            "continuous": true,
            "executor": "@nx/web:file-server",
            "options": {
                "buildTarget": "vehicles-app:build",
                "staticFilePath": "dist/vehicles-app/browser",
                "spa": true
            }
        }
    }
}
